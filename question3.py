# can we have a set with 18(int) and '18'(str) as a vaiue init
s=set()
s.add(18)
s.add('18')
print(s)
# yes, we can have a set with both 18 (int) and '18' (str) as 
# value in the set because they are of different data types.